# URL base de la API del backend
# Si usás el proxy de Vite (configurado en vite.config.ts), usá /api/v1
# Si no hay proxy, usá http://localhost:8000/api/v1
VITE_API_URL=http://localhost:8000/api/v1

# WebSocket URL base (debe apuntar directo al backend).
VITE_WS_URL=ws://localhost:8000
